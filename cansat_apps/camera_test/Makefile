
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_CANSAT_APPS_CAMERA_TEST_PROGNAME)
PRIORITY = $(CONFIG_CANSAT_APPS_CAMERA_TEST_PRIORITY)
STACKSIZE = $(CONFIG_CANSAT_APPS_CAMERA_TEST_STACKSIZE)
MODULE = $(CONFIG_CANSAT_APPS_CAMERA_TEST)

ASRCS =
CSRCS = camera_utils.c
MAINSRC = camera_test_main.c

include $(APPDIR)/Application.mk
