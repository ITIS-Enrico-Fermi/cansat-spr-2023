
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_CANSAT_APPS_FLIGHT_PROGNAME)
PRIORITY = $(CONFIG_CANSAT_APPS_FLIGHT_PRIORITY)
STACKSIZE = $(CONFIG_CANSAT_APPS_FLIGHT_STACKSIZE)
MODULE = $(CONFIG_CANSAT_APPS_FLIGHT)

ASRCS =
CSRCS =
MAINSRC = flight_main.c

include $(APPDIR)/Application.mk
