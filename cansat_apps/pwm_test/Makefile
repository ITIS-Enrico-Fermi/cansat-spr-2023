
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_CANSAT_APPS_PWM_TEST_PROGNAME)
PRIORITY = $(CONFIG_CANSAT_APPS_PWM_TEST_PRIORITY)
STACKSIZE = $(CONFIG_CANSAT_APPS_PWM_TEST_STACKSIZE)
MODULE = $(CONFIG_CANSAT_APPS_PWM_TEST)

ASRCS =
CSRCS =
MAINSRC = pwm_test_main.c

include $(APPDIR)/Application.mk
